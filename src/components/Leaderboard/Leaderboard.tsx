import React from "react";

import styles from './leaderboard.module';

const Leaderboard = () => {
    return (
        <div>
            Leaderboard
        </div>
    );
};

export default Leaderboard;
